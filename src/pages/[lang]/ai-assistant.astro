---
import AIAssistantPage from '@/pages/ai-assistant.astro';
import { supportedLocales } from '@/lib/i18n';

export const getStaticPaths = () =>
  supportedLocales
    .filter((locale) => locale !== 'zh')
    .map((lang) => ({
      params: { lang },
    }));
---

<AIAssistantPage />
