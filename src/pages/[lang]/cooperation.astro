---
import CooperationPage from '@/pages/cooperation.astro';
import { supportedLocales } from '@/lib/i18n';

export const getStaticPaths = () =>
  supportedLocales
    .filter((locale) => locale !== 'zh')
    .map((lang) => ({
      params: { lang },
    }));
---

<CooperationPage />
